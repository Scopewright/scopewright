# Session 2026-03-09 — Résumé

## Features livrées

- **#137 — Export PDF** (`shared/pdf-export.js` + `supabase/functions/pdf-export/index.ts`)
  - Migration html2pdf.js (client-side) → PDFShift API (server-side Chromium)
  - Nouvelle Edge Function `pdf-export` : proxy authentifié vers PDFShift API
  - Format landscape Letter, marges 0, `use_print: true`
  - Page total+signature reconstruite en layout 2 colonnes flex (design "Votre projet est prêt" de quote.html)
  - Page breaks CSS `:not(:first-child)` — fonctionne nativement avec Chromium
  - Document HTML autoportant envoyé (SNAPSHOT_CSS inline + Google Fonts)
  - Nom fichier : `{OrgName}_{ProjectCode}_{SubNumber}_v{Version}.pdf`
  - Supprimé : CDN html2pdf.js, html2canvas, conversion base64 images, hacks table-layout

- **#138 — Règle de présentation sur catégories de dépense**
  - `presentation_rule` JSON sur `expense_categories` (même structure que catalogue)
  - Nouvelle action AI `expense_pres_rule` dans edge function `translate`
  - Fallback calculateur : `_findGroupPresRule(catItem)` résout la règle du groupe

- **#138 suite — AI catégories de dépense (admin.html)**
  - Boutons AI (dot heartbeat) sur Template calcul et Template présentation
  - Nouveau champ `calc_rule` JSON persisté dans `expense_categories`
  - Fonctions : `aiExpenseCalcRule`, `aiExpensePresRule`

- **#139 — Style Guide UI**
  - Création `docs/STYLE_GUIDE.md` (17 sections, 244 lignes)
  - Section "Références obligatoires" dans CLAUDE.md
  - Directive : lire le style guide avant tout travail UI

- **#132 fix palette — Modal rentabilité alignée STYLE_GUIDE**
  - Barre répartition : navy (#0B1220), gris (#374151), gris clair (#9CA3AF), teal (#0D9488)
  - KPI Profit : teal OK, ambre warning
  - Bannière avertissement : fond ambre, bordure #F59E0B, texte #92400E
  - Barres MO : navy (#0B1220) sur fond #F1F5F9
  - Badges marges : teal/ambre/rouge
  - Retiré tout violet saturé (#A78BFA) et vert vif (#86EFAC, #22C55E)

## Bugs corrigés

- **#137 PDF pages blanches** — 3 fixes itératifs : injection SNAPSHOT_CSS dans `document.head`, DOM lifecycle html2pdf, CSS page-break `:not(:first-child)`
- **#137 PDF images blanches** — Conversion base64 des images cross-origin Supabase Storage
- **#137 PDF page signature** — Reconstruite en layout 2 colonnes matching quote.html
- **#137 PDF — 3 bugs critiques (v3)** — (1) Pages blanches : supprimé `page-break-after:always` redondant dans SNAPSHOT_CSS `@media print`. (2) HTML brut descriptions : ajouté `<br` à détection HTML dans `textToHtml()` + corrigé textarea→div `textContent`→`innerHTML`. (3) Texte coupé 2 colonnes : converti flex/grid en `display:table`/`table-cell` (room body, intro, why, signature)
- **#138 style admin incohérent** — Remplacement CSS custom par pattern catalogue modal (`.ai-field-wrap`, `.btn-ai-dot`)
- **Strip "prefix" AI** — Suppression du champ `prefix` halluciné par l'AI dans 3 chemins de réponse
- **`fixEnvelopeJsonSplit` clés** — Corrigé de `['sections','order','prefix']` vers `['sections','exclude','notes']`
- **Flèches scroll textareas admin** — Scrollbar native Windows supprimée via `::-webkit-scrollbar-button{display:none}` + `scrollbar-width:thin`
- **`loadAiPrompts()` whitelist** — `ai_prompt_labor_modifiers` manquait
- **Label "Façades et panneaux apparents"** — Résidu legacy scindé en "Façades" + "Panneaux" dans 5 fichiers code + seed SQL. Migration prod : `sql/fix_facades_label.sql`
- **#137 PDF — 3 bugs HTML source (post-PDFShift)** — (1) Pages blanches : override `aspect-ratio:unset;height:auto` sur `.pv-page` (l'aspect-ratio fixe de SNAPSHOT_CSS créait du blanc). (2) HTML brut : confirmé fix `textToHtml()` déjà en place (`<br` détecté). (3) Texte coupé : `overflow:hidden;max-width:100%;box-sizing:border-box` sur conteneurs 2 colonnes + `word-wrap:break-word` sur colonnes texte
- **Modifier % pièce — lignes cascade non rafraîchies** — `updateCollapsedParentTotal()` affichait les totaux sans multiplicateur room/global. Fix : applique `getModifierMultiplier(groupId)` au total affiché (collapsed aggregate et expanded individual)

## Décisions prises

- DEC-029 mise à jour : Export PDF server-side via PDFShift API (remplace html2pdf.js client-side). Chromium natif élimine tous les hacks html2canvas (table-layout, base64 images). Nouvelle Edge Function `pdf-export`
- Palette rentabilité alignée sur STYLE_GUIDE : teal (#0D9488) pour OK, ambre pour warning, interdit violet/vert vif
- Attio ajouté comme référence principale d'inspiration UI (tags compacts, vues multiples, densité d'information)
- Règle fondamentale formalisée : réutiliser les patterns UI existants, ne jamais réinventer

## Backlogs ouverts

- #124 — Node graph pour visualisation cascades (inspiré Attio)
- #138 plan — Barèmes et modificateurs (`labor_modifiers`) — plan complet rédigé, phases 1-9 définies

## Ce qui reste à faire

- Déployer Edge Function `pdf-export` : `npx supabase functions deploy pdf-export --no-verify-jwt`
- Configurer secret : `npx supabase secrets set PDFSHIFT_API_KEY=sk_...`
- Valider visuellement le PDF avec PDFShift (tester flex/grid, images, page breaks)
- Edge function `translate` déployée — vérifier en production
- Phase 2+ du plan #138 (barèmes) si priorisé
- Extraction `steleConfirm`/`steleAlert` dans shared/ (planifié, pas commencé)
- 3 prompts admin manquants dans le dropdown (`explication_catalogue`, `json_catalogue`, `approval_suggest`)

## Commits de la session

```
44f540f Fix #137 PDF — 2-column signature page + eliminate blank pages
f67eddb Update docs for #138 UI alignment fix + prefix stripping
352442f #138 fix — Align admin expense AI buttons with catalogue modal pattern
ff99ebd Update docs for #137 PDF layout fix + #138 suite admin AI buttons
f02fc3b #138 suite — Admin expense categories: AI buttons + JSON fields
59067de Fix #137 PDF layout — remove blank pages, black rectangle, redesign signature page
ae0b8fd Update docs for #138 — presentation_rule on expense categories
e00df2c #138 presentation_rule JSON on expense categories
```

## Fichiers modifiés

| Fichier | Action |
|---------|--------|
| `shared/pdf-export.js` | Réécrit : html2pdf → PDFShift via Edge Function (~170 lignes) |
| `supabase/functions/pdf-export/index.ts` | Créé — Edge Function PDFShift proxy (~95 lignes) |
| `admin.html` | +283 lignes (AI buttons, JSON fields, scrollbar fix) |
| `catalogue_prix_stele_complet.html` | Strip prefix, fix fixEnvelopeJsonSplit |
| `calculateur.html` | Palette rentabilité (#132 fix) |
| `supabase/functions/translate/index.ts` | Action `expense_pres_rule` + prompt |
| `docs/STYLE_GUIDE.md` | Créé (17 sections, 244 lignes) |
| `CLAUDE.md` | Section "Références obligatoires", palette rentab, line counts |
| `CHANGELOG.md` | Entrées 2026-03-09 |
| `docs/TECHNICAL_MANUAL.md` | PDF export, admin line count, palette rentab |
| `docs/USER_GUIDE.md` | PDF export, signature page description |
